usdt:/home/usr/.sdkman/candidates/java/current/lib/server/libjvm.so:hotspot:method__entry
{
    $class_name = str(arg1, arg2+1);
    $method_name = str(arg3, arg4+1);
    $signature = str(arg5);

    $filter_one = "HelloEBPF";
    $filter_two = "TestEBPF";
    
    if ($class_name == $filter_one || $class_name == $filter_two) {
        printf("%-20.20s %-20.20s %s\n", $class_name, $method_name, $signature);
    }
}

